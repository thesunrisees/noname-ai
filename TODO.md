# 无名AI v2.0 · 待开发路线图（TODO）

> 按优先级排序，P0 最高，P3 最低

---

## 🔥 P0 · 核心闭环验证

### 1. 多层网络训练实战验证
- **现状**：localTrainer.js 已升级用 trainOne 反向传播，但尚未验证训练后准确率
- **目标**：跑 1000+ 样本，验证训练后模型预测准确率 > 60%
- **验收标准**：
  - 训练后 predict() 输出与标签一致率 > 60%
  - 训练前后胜率对比提升 > 5%

### 2. 校准器实战验证
- **现状**：decisionCalibrator.js 已接入，但尚未打 10~20 局验证偏移累计
- **目标**：打 20 局，观察偏移是否合理累计，不会跑偏
- **验收标准**：
  - 累计偏移在 [-0.3, 0.3] 范围内
  - 偏移方向与实际胜率趋势一致

### 3. 元认知 6 维实战验证
- **现状**：metaCognition.js 已升级 6 维，但尚未验证不同模式/局面下认知变化
- **目标**：打几局，观察 familiar/modulator/level 是否随局面变化
- **验收标准**：
  - 熟悉局面 familiarity > 0.6
  - 陌生局面 familiarity < 0.4
  - intervention 级别与 familiarity 正相关

---

## ⚡ P1 · 功能完善

### 4. 决策回放可视化增强
- **现状**：decisionReplay.js + replayPanel.js 已完成基础版
- **待做**：
  - [ ] 支持单条决策详情展开（看完整候选列表）
  - [ ] 支持按干预级别筛选（已完成基础版）
  - [ ] 支持按时间范围筛选
  - [ ] 支持导出单局回放为 JSON
  - [ ] 支持对比两局回放差异

### 5. 模型热更新闭环验证
- **现状**：modelHotSwap.js 已完成，但尚未攒够 300 样本触发训练
- **待做**：
  - [ ] 攒够 300 样本，手动触发训练
  - [ ] 观察候选模型 A/B 测试结果
  - [ ] 验证晋升后模型胜率确实提升

### 6. 策略进化验证
- **现状**：evolution.js 已完成遗传算法，但尚未跑满 5 局触发第一次进化
- **待做**：
  - [ ] 打 5 局，触发第一次进化
  - [ ] 观察进化后种群适应度是否提升
  - [ ] 验证精英保留机制生效

### 7. 公共知识库验证
- **现状**：sharedKnowledge.js 已完成局面指纹 + 群体智慧
- **待做**：
  - [ ] 打 10+ 局，观察是否积累足够局面指纹
  - [ ] 验证高置信选择确实能提升胜率
  - [ ] 支持跨设备同步（未来）

---

## 🎯 P2 · 体验优化

### 8. 面板系统优化
- **现状**：所有面板都是全屏遮罩版
- **待做**：
  - [ ] 支持面板大小拖拽调整
  - [ ] 支持多面板并排显示
  - [ ] 支持面板主题切换（暗色/亮色）
  - [ ] 支持面板位置记忆

### 9. 决策对比增强
- **现状**：decisionCompare.js + comparePanel.js 已完成基础版
- **待做**：
  - [ ] 支持自定义对比档案（不只是预置 5 个）
  - [ ] 支持导出对比结果
  - [ ] 支持可视化对比（雷达图/柱状图）

### 10. 跨模式迁移验证
- **现状**：crossModeTransfer.js 已完成分层认知
- **待做**：
  - [ ] 在身份局攒样本后，切换到国战验证是否能迁移
  - [ ] 验证 autoPromote 是否正确提升全局认知
  - [ ] 支持手动指定哪些元素提升为全局

### 11. 权重持久化验证
- **现状**：weightPersist.js 已完成校准偏移固化
- **待做**：
  - [ ] 打 10+ 局，观察偏移是否正确固化进 B2
  - [ ] 验证固化后模型预测确实变化
  - [ ] 支持查看 B2 当前值

---

## 🔮 P3 · 未来方向

### 12. 协同学习跨设备同步
- **现状**：sharedKnowledge.js 是本地存储
- **未来**：
  - 支持同步到云端（多个玩家共享知识库）
  - 支持下载/上传知识库 JSON
  - 支持知识库版本管理

### 13. 策略进化高级版
- **现状**：evolution.js 是基础遗传算法
- **未来**：
  - 支持神经网络权重进化（不只是偏移）
  - 支持多目标进化（胜率 + 稳定性 + 多样性）
  - 支持与其他 AI 对战，用真实对手评估适应度

### 14. 模型热更新高级版
- **现状**：modelHotSwap.js 是本地训练 + A/B
- **未来**：
  - 支持在线学习（每局结束立即微调）
  - 支持联邦学习（多个玩家模型聚合）
  - 支持自动回滚（晋升后胜率下降自动回退）

### 15. 决策回放高级版
- **现状**：decisionReplay.js 是时间轴列表
- **未来**：
  - 支持可视化决策树（每条决策的分支）
  - 支持播放动画（逐帧展示决策过程）
  - 支持 AI 自动复盘（找出关键失误决策）

---

## 🐛 已知问题

### 1. 旧训练数据不兼容
- **问题**：模型权重还是 48 维时代的旧数据，新 96 维特征不匹配
- **解决**：执行 `window.__DJSC.trainBufferClear()` 清空旧数据
- **状态**：已知，待用户手动执行

### 2. GitHub 推送未完成
- **问题**：github_repo 已初始化，但用户尚未提供 GitHub 用户名和仓库名
- **状态**：等待用户提供

### 3. Python 蒸馏脚本未跑通
- **问题**：用户已在 Windows 桌面端部署，但尚未跑通第一次训练
- **状态**：等待用户验证

---

## 📊 里程碑

| 版本 | 目标 | 状态 |
|---|---|---|
| v1.0.0 | 基础规则打分引擎 | ✅ 已完成 |
| v1.1.0 | 元素反馈 + 元认知 + 校准 | ✅ 已完成 |
| v1.1.1 | 96维特征 + 多层网络 + 护栏 | ✅ 已完成 |
| **v2.0.0** | **完整闭环（学习/校准/进化/热更新）** | **✅ 已完成** |
| v2.1.0 | 实战验证 + 性能优化 | ⏳ 进行中 |
| v3.0.0 | 跨设备协同 + 云端知识库 | 🔮 规划中 |
