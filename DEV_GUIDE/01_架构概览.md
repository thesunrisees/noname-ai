# 架构概览

---

## 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                      无名AI 决策引擎                     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐ │
│  │  bestAction  │ -> │  评分循环    │ -> │  选最高分   │ │
│  │  (决策入口)  │    │  (枚举动作)  │    │  (执行出牌)  │ │
│  └─────────────┘    └─────────────┘    └─────────────┘ │
│                           │                              │
│                           ▼                              │
│  ┌───────────────────────────────────────────────────┐  │
│  │                  评分模块（24+ 模块）              │  │
│  ├───────────────────────────────────────────────────┤  │
│  │  handInference.js    - 对手手牌推断               │  │
│  │  keepStrategy.js      - 手牌保留策略               │  │
│  │  equipScarcity.js     - 装备稀缺度                 │  │
│  │  multiTurnPlan.js   - 多轮规划                   │  │
│  │  teammateIntent.js  - 队友意图分析               │  │
│  │  counterStrategy.js   - 反制策略                   │  │
│  │  situationEval.js    - 局势评估                   │  │
│  │  riskQuant.js        - 风险量化                   │  │
│  │  resourceManage.js   - 资源管理                   │  │
│  │  moodState.js        - 情绪状态                   │  │
│  │  identityVisual.js    - 身份推理                   │  │
│  │  replayAnalysis.js   - 回放分析                   │  │
│  │  mutualRelations.js  - 相互关系                   │  │
│  │  orderOptimizer.js   - 出牌顺序优化               │  │
│  │  passStrategy.js      - 传牌策略                   │  │
│  │  damageTransfer.js    - 伤害转移（铁索）          │  │
│  │  treeSearch.js         - 树形搜索                   │  │
│  │  delayOptimizer.js    - 延迟优化                   │  │
│  │  deckTopPredict.js    - 牌堆顶预测                 │  │
│  │  discardStrategy.js   - 弃牌策略                   │  │
│  │  learningLoop.js      - 学习闭环                   │  │
│  │  gameTheory.js        - 博弈论                   │  │
│  │  perfOptimizer.js     - 性能优化                   │  │
│  │  ...                  - 其他模块                   │  │
│  └───────────────────────────────────────────────────┘  │
│                           │                              │
│                           ▼                              │
│  ┌───────────────────────────────────────────────────┐  │
│  │                  数据层（记忆引擎）                │  │
│  ├───────────────────────────────────────────────────┤  │
│  │  deckMemory.js        - 牌堆记忆（147 张完整）     │  │
│  │  deckPredict.js       - 判定预测                   │  │
│  │  mem.js               - 短期记忆                   │  │
│  │  memory.js            - 长期记忆（跨局）          │  │
│  │  archive.js           - 归档                       │  │
│  │  modeStrategy.js      - 模式策略（11 种模式）     │  │
│  └───────────────────────────────────────────────────┘  │
│                           │                              │
│                           ▼                              │
│  ┌───────────────────────────────────────────────────┐  │
│  │                  训练系统（Int8）                  │  │
│  ├───────────────────────────────────────────────────┤  │
│  │  features.js          - 特征提取（48 维）          │  │
│  │  trainExport.js      - 训练数据缓冲               │  │
│  │  localTrainer.js     - 本地训练器                 │  │
│  │  weights.js           - 模型权重                  │  │
│  │  scoreUnify.js        - 统一值域（Int8）           │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 核心数据流

### 1. 决策流程

```
AI 回合开始
    ↓
bestAction(me)
    ↓
枚举候选动作（手牌、技能、装备、结束）
    ↓
对每个动作打分：
    s = 基础分
    s *= 风险系数
    s *= 局势系数
    s += 模块加分（24+ 模块）
    s = toInt8(s)  # 统一值域
    ↓
选最高分
    ↓
执行出牌
    ↓
记录决策（trainRecordSample）
```

### 2. 训练流程

```
打一局游戏
    ↓
每次决策记录样本：
    { f: [48维特征], r: 0 }  # reward 占位
    ↓
游戏结束
    ↓
回填 reward：
    所有样本的 r = 本局最终得分
    ↓
导出 JSON（可选）
    或
本地训练（localTrainer）
    ↓
保存权重到 localStorage
    ↓
下次开局自动加载
```

---

## 关键模块说明

### 1. engine.js（核心）

- **位置**：`score/engine.js`
- **职责**：决策引擎核心，bestAction 函数
- **大小**：~10000 行
- **关键函数**：
  - `bestAction(me)`：决策入口
  - `give(char, pts, tag)`：记分
  - `givePair(from, to, pts, tag)`：守恒记分
  - `giveVs(char, pts, tag)`：阵营记分
  - `settle()`：结算

### 2. scoreUnify.js（统一值域）

- **位置**：`score/scoreUnify.js`
- **职责**：统一值域为 Int8 [-127, 127]
- **关键函数**：
  - `toInt8(raw)`：浮点转 Int8
  - `toFloat(i8)`：Int8 转浮点

### 3. features.js（特征提取）

- **位置**：`score/features.js`
- **职责**：提取 48 维特征（32 维状态 + 16 维动作）
- **特征维度**：
  - f[0..7]：手牌
  - f[8..11]：血量
  - f[12..15]：身份
  - f[16..22]：场上局势
  - f[23..27]：装备
  - f[28..31]：阶段
  - f[32..35]：动作类型
  - f[36..41]：牌名映射
  - f[42..47]：目标特征

### 4. trainExport.js（训练数据缓冲）

- **位置**：`score/trainExport.js`
- **职责**：缓冲训练样本
- **关键函数**：
  - `pushSample(features, reward, meta)`：记录样本
  - `bufferSize()`：样本数
  - `bufferClear()`：清空
  - `downloadJson()`：导出 JSON

### 5. localTrainer.js（本地训练器）

- **位置**：`score/localTrainer.js`
- **职责**：本地训练 Int8 感知机
- **参数**：
  - EPOCHS = 60
  - LR_Q = 2
  - VAL_RATIO = 0.2
- **输出**：accuracy（准确率）

### 6. weights.js（模型权重）

- **位置**：`score/weights.js`
- **职责**：从 localStorage 加载权重
- **关键函数**：
  - `predict(features)`：预测
  - `saveLocalWeights(W, b, acc)`：保存权重
  - `reloadWeights()`：重新加载

---

## 数据流总结

| 阶段 | 输入 | 输出 | 模块 |
|------|------|------|------|
| 决策 | 当前局势 | 最佳动作 | engine.js + 24 模块 |
| 记分 | 动作 + 结果 | 分数 | give/givePair/giveVs |
| 特征 | 动作 + 局势 | 48 维特征 | features.js |
| 训练 | 样本 + reward | 权重 | localTrainer.js |
| 推理 | 特征 | 预测 | weights.js |

---

## 性能考虑

1. **零对象分配**：features.js 用 Float32Array/Int8Array 复用
2. **缓存**：deckMemory.js 维护缓存，避免重复遍历
3. **延迟计算**：deckPredict.js 用 1 秒缓存
4. **异步训练**：localTrainer.js 用 setTimeout 异步执行
